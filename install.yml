---
- name: Install httpd
  hosts: all
  tasks:
    - block:
      - name: install httpd
        yum: name=httpd state=latest
    - block:
      - name: start httpd service
        service: name=httpd state=started
