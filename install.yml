---
- name: Install httpd
  hosts: all
  tasks:
    - block:
      - name: install httpd
        yum: name=httpd state=latest
    - block:
      - name: Start httpd service
        service: httpd
        status: started
